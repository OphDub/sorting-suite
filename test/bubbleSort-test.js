const bubbleSort = require('../bubbleSort.js');
const chai = require('chai');
const expect = chai.expect;

describe('Bubble Sort', function() {
  it('expects to equal true', () => {
    expect(true).to.equal(true);
  });

  
});